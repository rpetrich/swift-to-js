public func arrayIncrement(array: inout [Int], index: Int, amount: Int) {
	array[index] += amount
}

public func arrayDecrement(array: inout [Int], index: Int, amount: Int) {
	array[index] -= amount
}
